# Ralph workflow

The active unit is `US-001` in `prd.json`.

Definition of done:

1. A Violentmonkey script targets only Zapia Chat.
2. It provides accessible prefix actions, multi-select controls, and a selected-count toolbar.
3. It opens only native Zapia deletion entry points, never the final confirmation.
4. Deterministic DOM-fixture tests pass.
5. Browser validation is recorded after installation in an authenticated LibreWolf session.
